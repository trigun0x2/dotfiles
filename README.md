jeff's dotfiles
===================

Requirements
------------

Set zsh as your login shell:

    chsh -s $(which zsh)

Install
-------

    Use stow to install files
    Do the following for each config you want
    `stow zsh`

Install (old):
--------------

    ./install.sh
